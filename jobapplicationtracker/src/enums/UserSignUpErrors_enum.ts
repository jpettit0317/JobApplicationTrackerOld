enum UserSignUpErrors {
    userNameEmpty = `Username shouldn't be empty.`,
    emailEmpty = `Email shouldn't be empty.`,
    passwordEmpty = `Password shouldn't be empty.`,
    confirmedPasswordEmpty = `Confirm password shouldn't be empty.`,
    passwordsDontMatch = `Passwords don't match`
}

export default UserSignUpErrors;